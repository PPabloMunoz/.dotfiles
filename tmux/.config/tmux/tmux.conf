###############################################################################
# Configuration
###############################################################################

source "~/.config/tmux/config/options.conf"
source "~/.config/tmux/config/keybindings.conf"

###############################################################################
# Plugins
###############################################################################

set-environment -g TMUX_PLUGIN_MANAGER_PATH "~/.config/tmux/plugins"

set -g @plugin "tmux-plugins/tpm"
set -g @plugin "tmux-plugins/tmux-sensible" # Sensible default values

set -g @plugin 'catppuccin/tmux'                 # beautiful status bar (optional)
set -g @catppuccin_flavour 'mocha'

set -g @plugin "MunifTanjim/tmux-mode-indicator" # Mode Indicator
set -g @plugin "tmux-plugins/tmux-resurrect" # Restore tmux sessions
set -g @plugin "tmux-plugins/tmux-continuum" # Continuous saving of tmux environment - MUST BE LAST

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run "~/.config/tmux/plugins/tpm/tpm"
